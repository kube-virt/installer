To verify that kubevirt-infra-csi-driver has started, run:

  kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "kubevirt-infra-csi-driver.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"
